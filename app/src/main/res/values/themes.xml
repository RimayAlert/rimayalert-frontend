<resources>

    <style name="Theme.RimayAlert" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.RimayAlert.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.RimayAlert</item>
    </style>

</resources>
